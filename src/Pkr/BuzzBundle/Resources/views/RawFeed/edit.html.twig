{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block headline %}Edit raw feed{% endblock %}

{% block content %}
    <div class="row">
        <div class="span8">
            <p class="margin-bottom">
                <a class="btn" href="{{ path('rawfeed', { 'id': entity.category.id }) }}">
                    Back
                </a>
            </p>
        </div>
        <div class="span4">
            <form
                class="pull-right"
                action="{{ path('rawfeed_delete', { 'id': entity.id }) }}"
                method="post"
            >
                {{ form_widget(delete_form) }}
                <button class="btn btn-danger" type="submit">Delete</button>
            </form>
        </div>
    </div>

    <form action="{{ path('rawfeed_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>
{% endblock %}
