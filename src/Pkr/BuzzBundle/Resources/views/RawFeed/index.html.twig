{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block content %}
    <h1>RawFeed list of Category '{{ category.name }}'</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Url</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in rawFeeds %}
            <tr>
                <td><a href="{{ path('rawfeed_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.url }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('rawfeed_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('rawfeed_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rawfeed_new', { 'id': category.id }) }}">
                Create a new entry
            </a>
        </li>
        <li>
            <a href="{{ path('category_show', { 'id': category.id }) }}">
                Back to category
            </a>
        </li>
    </ul>
{% endblock %}
