{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block headline %}Create raw feed{% endblock %}

{% block content %}
    <div class="row">
        <div class="span12">
            <p class="margin-bottom">
                <a class="btn" href="{{ path('rawfeed', { 'id': entity.category.id }) }}">
                    Back
                </a>
            </p>
        </div>
    </div>

    <form action="{{ path('rawfeed_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>
{% endblock %}
