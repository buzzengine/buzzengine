{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block headline %}Create feed{% endblock %}

{% block content %}
    <div class="row">
        <div class="span12">
            <p class="margin-bottom">
                <a class="btn" href="{{ path('feed', { 'id': entity.category.id }) }}">
                    Back
                </a>
            </p>
        </div>
    </div>

    <form action="{{ path('feed_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>
{% endblock %}
