{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block headline %}Feeds of category '{{ category.name }}'{% endblock %}

{% block content %}
    <div class="row">
        <div class="span12">
            <p class="margin-bottom btn-group">
                <a class="btn" href="{{ path('category') }}">
                    Back
                </a>
                <a class="btn" href="{{ path('feed_new', { 'id': category.id }) }}">
                    Create feed
                </a>
            </p>
        </div>
    </div>

    <table class="table table-striped">
        <thead>
            <tr>
                <th></th>
                <th>Url</th>
                <th>Disabled</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in feeds %}
            <tr>
                <td><span class="badge">{{ entity.id }}</span></td>
                <td>{{ entity.url }}</td>
                <td>{% if entity.disabled == true %}true{% else %}false{% endif %}</td>
                <td>
                    <div class="btn-group pull-right">
                        <a class="btn btn-small" href="{{ path('feed_edit', { 'id': entity.id }) }}">Edit</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
