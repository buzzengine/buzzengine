{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="span8">
            <p class="margin-bottom">
                <a class="btn" href="{{ path('topicFeed', { 'id': entity.topic.id }) }}">
                    Back
                </a>
            </p>
        </div>
        <div class="span4">
            <form
                class="pull-right"
                action="{{ path('topicFeed_delete', { 'id': entity.id }) }}"
                method="post"
            >
                {{ form_widget(delete_form) }}
                <button class="btn btn-danger" type="submit">Delete</button>
            </form>
        </div>
    </div>

    <h2>TopicFeed edit</h2>

    <form action="{{ path('topicFeed_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>
{% endblock %}
