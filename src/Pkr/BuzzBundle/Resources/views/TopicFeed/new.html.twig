{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block headline %}Create topic feed{% endblock %}

{% block content %}
    <div class="row">
        <div class="span12">
            <p class="margin-bottom">
                <a class="btn" href="{{ path('topicFeed', { 'id': entity.topic.id }) }}">
                    Back
                </a>
            </p>
        </div>
    </div>

    <form action="{{ path('topicFeed_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>
{% endblock %}
