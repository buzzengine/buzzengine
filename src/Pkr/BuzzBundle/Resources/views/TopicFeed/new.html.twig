<h1>TopicFeed creation</h1>

<form action="{{ path('topicFeed_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('topicFeed', { 'id': entity.topic.id }) }}">
            Back to the list
        </a>
    </li>
</ul>
