{% extends 'PkrBuzzBundle::base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="span12">
            <p class="margin-bottom btn-group">
                <a class="btn" href="{{ path('topic') }}">
                    Back
                </a>
                <a class="btn" href="{{ path('topicFeed_new', { 'id': topic.id }) }}">
                    Create topic feed
                </a>
            </p>
        </div>
    </div>

    <h2>TopicFeed list of Topic '{{ topic.name }}'</h2>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Url</th>
                <th>Disabled</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in topicFeeds %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.url }}</td>
                <td>{% if entity.disabled == true %}true{% else %}false{% endif %}</td>
                <td>
                    <div class="btn-group pull-right">
                        <a class="btn btn-small" href="{{ path('topicFeed_edit', { 'id': entity.id }) }}">Edit</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
